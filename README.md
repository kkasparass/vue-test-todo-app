# Test Vue Project

This is a test TODO app project to get to know Vue. The base was created with `npm create vue@latest`

## Running the Project

Install dependencies: `npm install`
Start the development server: `npm run dev`

## Running Tests

Run tests in watch mode: `npm run test`
